{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Serve",
            "type": "shell",
            "command": "dart run build_runner serve"
        },
        {
            "label": "Serve (debug)",
            "type": "shell",
            "command": "dart run build_runner serve --config debug"
        },
        {
            "label": "Build",
            "type": "shell",
            "command": "dart run build_runner build"
        },
        {
            "label": "Build (debug)",
            "type": "shell",
            "command": "dart run build_runner build --config debug"
        }
    ]
}